{
  "name": "paintings-and-artists",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "db:delete": "dropdb artist-browser",
    "db:create": "createdb artist-browser",
    "db:schema": "psql -f schema.sql artist-browser",
    "db:seed": "psql -f seed.sql artist-browser",
    "db:reset": "npm run db:delete && npm run db:create && npm run db:schema && npm run db:seed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
